# Server Port (optional)
server.port=8080

# Enable sessions
server.servlet.session.timeout=30m

# Logging
logging.level.org.springframework.web=INFO

# Allow loading static HTML files
spring.web.resources.static-locations=classpath:/static/
